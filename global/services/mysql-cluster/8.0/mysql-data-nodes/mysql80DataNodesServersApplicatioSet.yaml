apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: mysql-data-nodes-manager
  namespace: argocd
spec:
  generators:
    - git:
        directories:
          - path: global/services/mysql-cluster/8.0/mysql-data-nodes/chart/values/*
        repoURL: 'https://github.com/sabababoy/labondemand'
        revision: HEAD
  template:
    metadata:
      name: '{{path.basename}}.mysql-data-nodes'
    spec:
      destination:
        namespace: argocd
        server: 'https://kubernetes.default.svc'
      project: default
      source:
        repoURL: 'https://github.com/sabababoy/labondemand.git'
        targetRevision: HEAD
        path: global/services/mysql-cluster/8.0/mysql-data-nodes/chart
        helm:
          valueFiles:
            - 'values/{{path.basename}}/values.yaml'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true


